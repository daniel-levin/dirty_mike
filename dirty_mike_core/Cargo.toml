[package]
name = "dirty_mike_core"
version = "0.1.0"
edition = "2024"
description = "Quick and dirty self-instrumentation for Rust"
license = "Unlicense"

[dependencies]
perf-event2 = "0.7.4"
dirty_mike_derive = { version = "0.1", path = "../dirty_mike_derive" }
thiserror = "2"
derive_more = { version = "2.0.1", features = ["debug"] }
metrics = "0.24.2"

[dev-dependencies]
anyhow = "1"
rand = "0.9.2"
